

- <font color="#ffff00">type= FilterType.ANNOTATION</font>은 기본값이라 <font color="#ffff00">생략</font> 가능

``` java title:"필터링을 하는 테스트 코드 - BeanA, BeanB", hl:24,30-37
package scan.filter;  
  
import org.assertj.core.api.Assertions;  
import org.junit.jupiter.api.Test;  
import org.springframework.beans.factory.NoSuchBeanDefinitionException;  
import org.springframework.context.annotation.AnnotationConfigApplicationContext;  
import org.springframework.context.annotation.ComponentScan;  
import org.springframework.context.annotation.Configuration;  
import org.springframework.context.annotation.FilterType;  
import org.springframework.stereotype.Component;  
  
import static org.assertj.core.api.Assertions.*;  
import static org.springframework.context.annotation.ComponentScan.*;  
  
public class ComponentFilterAppConfigTest {  
  
    @Test  
    void filterScan(){  
        AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(ComponentFilterAppConfig.class);  
        BeanA beanA = ac.getBean("beanA", BeanA.class);  
  
        assertThat(beanA).isNotNull();  
  
        // 이걸 조회하는 순간 Exeoption이 터진다 -> 등록이 안되어 있으니까(@MyExcludeComponent를 달아놓은 클래스니까 -> 아래에서 excludeFilter로 해당 어노테이션이 붙으면 등록이 안되게 해놓았음.)  
        org.junit.jupiter.api.Assertions.assertThrows(  
                NoSuchBeanDefinitionException.class,  
                () ->ac.getBean("beanB",BeanB.class));  
    }  
  
    @Configuration  
    @ComponentScan(  
            includeFilters = @Filter(type= FilterType.ANNOTATION, classes = MyIncludeComponent.class),  
            excludeFilters = @Filter(type= FilterType.ANNOTATION, classes = MyExcludeComponent.class)  
    )  
    static class ComponentFilterAppConfig{  
  
    }  
}
```

- 우리가 등록해 놓은 어노테이션 @MyIncludeComponent와 @MyExcludeComponent 중에서 @Configuration에서 @MyExcludeComponet는 필터링에서 excludeFilter 해서 막아놓았음.
- beanB는 아래와 같이 필터링된 어노테이션을 붙여서 빈 등록 자체가 안됨.
```java title:"BeanB"
package scan.filter;  
  
@MyExcludeComponent  
public class BeanB {  
}
```


![[Pasted image 20241126220428.png]]
![[Pasted image 20241126220440.png]]
- 솔직히 요즘 그렇게까지 쓸 일은 없음 -> include는 특히
- 최대한 스프링이 제공하는 기본 설정을 사용하는 것이 권장.