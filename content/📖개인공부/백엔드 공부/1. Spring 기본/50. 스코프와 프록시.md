

### @Scope에서 proxyMode = ScopedProxyMode.TARGET_CLASS
```java title:"프록시 모드를 지정", hl:13
package hello.example.core.common;  
  
  
import jakarta.annotation.PostConstruct;  
import jakarta.annotation.PreDestroy;  
import org.springframework.context.annotation.Scope;  
import org.springframework.context.annotation.ScopedProxyMode;  
import org.springframework.stereotype.Component;  
  
import java.util.UUID;  
  
@Component  
@Scope(value = "request", proxyMode = ScopedProxyMode.TARGET_CLASS)  
public class MyLogger {  
  
    private String uuid;  
    private String requestURL;  
    public void setRequestURL(String requestURL) {  
        this.requestURL = requestURL;  
    }  
  
    public void log(String message){  
        System.out.println("[ "+uuid+" ]"+"[ "+requestURL+" ]"+"[ "+message+" ]");  
    }  
  
    @PostConstruct  
    public void init(){  
        uuid = UUID.randomUUID().toString();  
        System.out.println("[ "+uuid+" ]"+"request scope bean create : "+this);  
    }  
  
    @PreDestroy  
    public void close(){  
        System.out.println("[ "+uuid+" ]"+"request scope bean close : "+this);  
    }  
  
}
```

```java title:"프록시 모드 지정 후 코드 - Controller"
package hello.example.core.web;  
  
import hello.example.core.common.MyLogger;  
import jakarta.servlet.http.HttpServletRequest;  
import lombok.RequiredArgsConstructor;  
import org.springframework.beans.factory.ObjectProvider;  
import org.springframework.stereotype.Controller;  
import org.springframework.web.bind.annotation.RequestMapping;  
import org.springframework.web.bind.annotation.ResponseBody;  
  
@Controller  
@RequiredArgsConstructor  
public class LogDemoController {  
  
    private final LogDemoService logDemoService;   
    private final MyLogger myLogger;  
  
    @RequestMapping("log-demo")  
    @ResponseBody  
    public String logDemo(HttpServletRequest request){  
        String requestURL = request.getRequestURL().toString();  
        myLogger.setRequestURL(requestURL);  
  
        myLogger.log("controller test");  
        logDemoService.logic("testId");  
  
        return "OK";  
    }  
}
```

```java title:"프록시 모드 적용 후 코드 - Service"
package hello.example.core.web;  
  
import hello.example.core.common.MyLogger;  
import lombok.RequiredArgsConstructor;  
import org.springframework.beans.factory.ObjectProvider;  
import org.springframework.stereotype.Service;  
  
@Service  
@RequiredArgsConstructor  
public class LogDemoService {  
   
    private final MyLogger myLogger;  
    public void logic(String id) {  
        myLogger.log("service id = "+id);  
    }  
}
```


- 뭐 제대로 잘 돌아간다.
- <font color="#ffff00">MyLogger의 가짜 프록시 클래스를 만들어두고, HTTP request와 상관없이 가짜 프록시 클래스를 다른 빈에 미리 주입할 수 있다!</font>

![[Pasted image 20241231001452.png]]
- 이걸 보면 스프링 컨테이너에서 만들 때, SptringCGLIB를 통해서 <u>껍데기 뿐인</u> myLogger를 가지고 있다.


![[Pasted image 20241231001606.png]]
![[Pasted image 20241231001726.png]]
- <font color="#ffff00">의존관계 주입까지도 이 가짜 프록시 객체가 주입!</font>
	- 진짜 사용하는 시점에, 진짜를 스프링 컨테이너가 찾아옴.
- 가짜 프록시 객체가 myLogger.logic()를 진짜 객체를 찾아서 호출해줌

___

### 동작 정리

- CGLIB라는 라이브러리를 사용해서 바이트 코드를 조작한 가짜 프록시 객체를 주입
- 이 가짜 프록시 객체는 실제 요청이 들어오면, 실제 빈을 요청하는 위임 로직이 있음
- 가짜 프록시 객체는 실제 request scope와는 관계가 없음 -> 단순히 위임 로직만 있음 -> 마치 싱글톤처럼 동작함

___
### 특징 정리

- 마치 싱글톤 빈을 사용하듯이 request scope를 사용할 수 있다
- <font color="#ffff00">사실 핵심은 실제로 HTTP request가 들어오기 전까지 지연을 시키는 것이다</font>
- 단지 어노테이션 설정만으로 프록시 객체로 대체 -> <font color="#ffff00">다형성과 DI컨테이너의 강점</font>
	- <span style="background:#fff88f">클라이언트 코드를 전혀 바꿀 필요가 없다!!!</span>
- 꼭 웹 스코프가 아니여도 프록시는 사용가능하다.

___
### 주의점

- 마치 싱글톤 같기만, 다르게 동작한다 -> 요청마다 따로 생성
- 이런 특별한 scope는 꼭 필요한 부분에서만 부분적으로 사용하자