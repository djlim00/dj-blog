

새로운 할인정책을 추가해보자..
	: DIP, OCP를 지킬 수가 없음 -> 이 문제를 해결하기 위해서 여러가지 과정을 거침 -> 이 과정에서 스프링 컨테이너가 왜 필요한가?를 알 수 있게 됨 -> 스프링으로 간단하게 전환이 가능함을 확인해보자.


### OrderServiceImple 코드만 이렇게 바꾸면 되지 않나>?
![[Pasted image 20241122203657.png]]


1. 우리는 역할과 구현을 충실하게 분리함. ok
2. 다형성을 활용하고, 인터페이스와 구현 객체를 분리 했다. ok

3. <span style="background:#d4b106">OCP, DIP 객제지향 설계 원칙을 준수했다?? ----> NO!</span>
	- <font color="#ff0000">OCP(개방/폐쇄 원칙) 위반</font> -> 클라이언트 코드인 OrderService의 구현체인 OrderServiceImple을 수정했음 -> 변경이 일어남!
	- <font color="#ff0000">DIP(의존관계 역전 원칙) 위반</font> -> 현재 지금 주문서비스 클라이언트 DiscountPolicy 인터페이스만 의존한거 아닌가? 
		-> 아니다! 지금 보면 Fixed.. , Rate...(구현체) 에도 의존하고 있음을 볼 수 있음.
		-> 추상체에만 의존하려면 진짜 DiscountPolicy 인터페이스만 의존해야함. 위의 예시처럼 FixDiscountPolicy or RateDiscountPolicy(구현체)에도 의존하고 있음. 그냥 추상체 구현체 둘 다 의존하고 있는 셈.
![[Pasted image 20241122204434.png]]
- OrderServiceImpe의 코드를 바꾼다는게 의존성이 있다는 뜻이다.

___
# 어떻게 문제를 해결할까? - 코드를 안고치고..?

정말로 추상(인터페이스)에만 의존하도록 해야함.

#### 이렇게..?  ---> 당연히 null pointer Exeption 터짐
![[Pasted image 20241122204734.png]]


#### <span style="background:#d4b106">이 문제를 해결하려면 클라이언트인 'OrderServiceImpl'에 'Discountpolicy'의 구현 객체를 대신 생성해서 주입해줘야함</span>
