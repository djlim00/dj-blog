3
- 테스트 코드를 이용해서 확인해 보자

``` java title:"ApplicationContextTest - 빈 조회 테스트 코드", hl:24  
public class ApplicationContextInfoTest {  
  
  
    AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(AppConfig.class);  
  
    @Test  
    @DisplayName("모든 빈 출력하기")  
    void findAllBean() {  
        String[] beanDefinitionNames = ac.getBeanDefinitionNames();  
        for (String beanDefinitionName : beanDefinitionNames) {  
            Object bean = ac.getBean(beanDefinitionName);  
            System.out.println("name = " + beanDefinitionName + "object = " + bean);  
        }  
  
    }  
  
    @Test  
    @DisplayName("애플리케이션 빈(우리가 생성한빈) 출력하기")  
    void findApplicationBean() {  
        String[] beanDefinitionNames = ac.getBeanDefinitionNames();  
        for (String beanDefinitionName : beanDefinitionNames) {  
            BeanDefinition beanDefinition = ac.getBeanDefinition(beanDefinitionName);  
  
            if (beanDefinition.getRole() == BeanDefinition.ROLE_APPLICATION) {  
                Object bean = ac.getBean(beanDefinitionName);  
                System.out.println("name = " + beanDefinitionName + "object = " + bean);  
            }  
        }  
  
    }  
}
```

- AnnotationConfigApplicationConext(어노테이션 컨피그 어플리케이션 컨텍스트)을 선언해서 컨테이너 객체를 생성
- ac를 통해서 등록된 bean객체를 확인할 수 있음.
- beanDefinitionName은 이름은 String[] 형식으로 가져옴.
- <u>beamDefinition을 통해서 빈 객체의 정의를 가져오고 거기서 빈의 역할을 알 수 있음</u>.
	- getRole()을 통해서 이게 applicationBean인지 확인
	- <font color="#ffff00"> ROLE_APPLICAION(우리가 만든 것), ROLE_INGRASTRUCTURE(스프링 내부적으로 필요한 빈)</font>


___
``` java title:"ApplicationContextBasicFindTest - 기본 조회 기능"
package hello.example.core.beanfind;  
  
import hello.example.core.AppConfig;  
import hello.example.core.member.MemberService;  
import hello.example.core.member.MemberServiceImpl;  
import org.assertj.core.api.Assertions;  
import org.junit.jupiter.api.DisplayName;  
import org.junit.jupiter.api.Test;  
import org.springframework.beans.factory.NoSuchBeanDefinitionException;  
import org.springframework.context.annotation.AnnotationConfigApplicationContext;  
  
import java.lang.annotation.Annotation;  
  
import static org.assertj.core.api.Assertions.*;  
import static org.junit.jupiter.api.Assertions.*;  
  
public class ApplicationConextBasicFindTest {  
  
    AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(AppConfig.class);  
  
    @Test  
    @DisplayName("빈 이름으로 조회")  
    void findBeanyName() {  
        MemberService memberService = ac.getBean("memberService", MemberService.class);  
//        System.out.println("memberService = " + memberService);  
//        System.out.println("memberService.getClass() = " + memberService.getClass());  
        assertThat(memberService).isInstanceOf(MemberServiceImpl.class);  
    }  
  
    @Test  
    @DisplayName("이름 없이 타입으로만 조회")  
    void findBeanyType() {  
        MemberService memberService = ac.getBean(MemberService.class);
	assertThat(memberService).isInstanceOf(MemberServiceImpl.class);  
    }  
  
    // 딱히 좋은 방법은 아님 - 역할에 의존해야하므로  
    @Test  
    @DisplayName("구체 타입으로 조회")  
    void findBeanByName2() {  
        MemberService memberService = ac.getBean("memberService", MemberServiceImpl.class);  
        assertThat(memberService).isInstanceOf(MemberServiceImpl.class);  
    }  

	// 실패 케이스
    @Test  
    @DisplayName("빈 이름으로 조회X")  
    void findBeanByNameX() {  
        // ac.getBean("xxxx", MemberService.class);  
        // 위의 로직을 실행하면 저런 객체는 없으니까 아래의 Execption이 발생한다.  
        assertThrows(NoSuchBeanDefinitionException.class,  
                () -> ac.getBean("xxxx", MemberService.class));  
    }  
}

```

- static import 단축키 : 알트 + 엔터
- 복사해서 바로 아래 붙이기 : crl + D

