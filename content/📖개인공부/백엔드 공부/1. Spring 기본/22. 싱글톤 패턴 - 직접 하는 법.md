
- <font color="#ffff00">클래스의 인스턴스가 딱 1개만 생성되는 것을 보장하는 디자인 패턴</font>
- 객체 인스턴스를 2개 이상 생성되지 못하도록 막아야함
	: <font color="#ffff00">private생성자</font>를 사용해서 외부에서 임의로 new키워드를 사용하지 못하도록 막아야한다.


```java title:"싱글톤 패턴이 적용된 코드"
public class SingletonService {  
  
    // 자기 자신을 static으로 선언함. 클래스 레벨에 올라오기 때문에 하나만 존재하게 된다. 음음 맞지  
    private static final SingletonService instance = new SingletonService();  
  
    public static SingletonService getInstance() {  
        return instance;  
    }  
  
  
    private SingletonService() {  
    }  
  
    public void logic() {  
        System.out.println("싱글톤 객체 로직 호출");  
    }  
}
```

1. static 영역에 객체 instance를 미리 하나 생성해서 올려둔다.
2. 이 객체 인스턴스가 필요하면 오직 'getInstance()'메서드를 통해서만 조회 가능. 이 메서드를 호출하면 항상 같은 인스턴스를 반환(아까 올려둔 instance)
3. 딱 1개의 객체 인스턴스만 존재해야 하므로, 생성자를 Private으로 막아서 혹시라도 외부에서 new 키워드로 객체 인스턴스가 생성되는 것을 막는다.


```java title:"싱글톤 패턴이 적용 안된 경우 vs 싱글톤 패턴", hl:29
public class SingletonTest {  
  
    @Test  
    @DisplayName("스프링 없는 순수한 DI컨테이너")  
    void pureContainer() {  
        AppConfig appConfig = new AppConfig();  
  
        // 1. 조회 : 호출할 때 마다 객체를 생성  
        MemberService memberService1 = appConfig.memberService();  
  
        // 2. 조회 : 호출할 때 마다 객체를 생성  
        MemberService memberService2 = appConfig.memberService();  
  
        // 참조값이 다를 것을 확인  
        System.out.println("memberService1 = " + memberService1);  
        System.out.println("memberService2 = " + memberService2);  
  
        assertThat(memberService1).isNotSameAs(memberService2);  
    }  
  
    @Test  
    @DisplayName("싱글톤 페턴을 적용한 객체 사용")  
    void singletonServiceTest() {  
        SingletonService singtonService1 = SingletonService.getInstance();  
        SingletonService singtonService2 = SingletonService.getInstance();  
  
        System.out.println("singtonService2 = " + singtonService2);  
        System.out.println("singtonService1 = " + singtonService1);  
        assertThat(singtonService1).isSameAs(singtonService2);  
    }  
}
```
- 위의 경우는 new, 아래는 싱글톤 패턴으로 <span style="background:#d4b106">getInstance()</span>
- 아래는 당연한게, singletonService에서 단 한개를 만들어 놓은 instance를 getInstance()를 가지고 호출하기 때문에, 객체가 다를리가 없다.

```java title:"결과", hl:7-8

// 싱글톤 패턴이 없는 경우 새로운 인스턴스를 생성
memberService1 = hello.example.core.member.MemberServiceImpl@197d671
memberService2 = hello.example.core.member.MemberServiceImpl@402e37bc

// 싱글톤 패턴이 적용된 경우 같은 인스턴스를 참조
singtonService2 = hello.example.core.singleton.SingletonService@5a955565
singtonService1 = hello.example.core.singleton.SingletonService@5a955565
```

- <font color="#ffff00">isSameAs() : 인스턴스 비교</font>
- <font color="#ffff00">isEqualTo() : 자바의 == 연산</font>



## 그렇다면 우리는 기존의 AppConfig를 다 싱글톤 패턴으로 바꿔야하나...? 
# -> <font color="#ffff00">NO! 스프링에서 알아서 싱글톤 패턴으로 만들어준다</font>

____
>**싱글톤 패턴의 문제**
>- 의존관계상 클라이언트가 구체 클래스에 의존한다. -> DIP위반(추상화에 의존하지 않음)
>- 클라이언트가 구체 클래스에 의존하면 OCP를 위반할 가능성이 높음(바꾸면 바꿔야함)
>- 내부 속성 변경이 어려움
>- 안티패턴으로 불리기도 함..(안정적이지만 수많은 단점이 있음)
___



# 그렇지만 스프링은 이러한 문제점을 전부 해결함! 



