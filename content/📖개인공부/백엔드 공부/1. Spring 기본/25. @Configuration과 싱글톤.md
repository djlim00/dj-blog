
- @Configuration은 싱글톤을 위해서 존재한다!

![[Pasted image 20241126162438.png]]
위의 코드를 보자. 
memberService() -> new MemoryMemberRepository()
orderService() -> new MemoryMemberRepository()

<span style="background:#d4b106">이렇게 되면 두번 호출되게 되는데, 과연 이게 싱글톤 패턴이 깨질까?</span>

``` java title:"다른 곳에서 여러번 호출해도 싱글톤이 지켜지는 테스트 코드"
public class ConfigurationSingletonTest {  
  
    @Test  
    void configurationTest() {  
        ApplicationContext ac = new AnnotationConfigApplicationContext(AppConfig.class);  
  
        MemberServiceImpl memberService = ac.getBean("memberService", MemberServiceImpl.class);  
        OrderServiceImpl orderService = ac.getBean("orderService", OrderServiceImpl.class);  
        MemberRepository memberRepository = ac.getBean("memberRepository", MemberRepository.class);  
  
        MemberRepository memberRepository1 = memberService.getMemberRepository();  
        MemberRepository memberRepository2 = orderService.getMemberRepository();  
  
        System.out.println("memberRepository1 = " + memberRepository);  
        System.out.println("memberRepository2 = " + memberRepository1);  
        System.out.println("memberRepository = " + memberRepository);  
  
        assertThat(memberRepository).isSameAs(memberRepository1);  
        assertThat(memberService.getMemberRepository()).isSameAs(memberRepository);  
        assertThat(orderService.getMemberRepository()).isSameAs(memberRepository);  
          
    }  
}
```

``` title:결과
memberRepository1 = hello.example.core.member.MemoryMemberRepository@35293c05
memberRepository2 = hello.example.core.member.MemoryMemberRepository@35293c05
memberRepository = hello.example.core.member.MemoryMemberRepository@35293c05
```

- 전부 똑같은 것을 참조한다!