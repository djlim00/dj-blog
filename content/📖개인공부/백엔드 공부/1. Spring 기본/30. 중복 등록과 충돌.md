
- 만약에 MemoryServiceImpl의 컴포넌트 이름을 @Component("Service")로 지정하고, OrderServiceImpl의 컴포넌트 이름도 @Component("Service")로 지정하면 어떻게 될까?

<오류내역>
Failed to parse configuration class [hello.example.core.AutoAppConfig]
Annotation-specified bean name 'Service' for bean class [hello.example.core.order.OrderServiceImpl]


### 0. 그렇다면  "수동 빈 등록 vs 자동 빈 등록" 의 상황에서는?

<font color="#ffff00">성공은 한다..? 수동 빈 등록이 우선권을 가진다, 수동빈이 자동빈을 오버라이드 한다.</font>

- 현실적으로는 여러 설정들이 꼬여서 결과가 이상해지는 경우가 있다.
	"가장 어려운 버그는 애매한 버그다"
- <span style="background:#d4b106">이러한 애매한 상황을 만들지를 말자!</span>

- 그래서 요즘은 스프링부트에서 실행하면 중복된 경우가 생기면 오류를 발생시킨다.
![[Pasted image 20241126223711.png]]


