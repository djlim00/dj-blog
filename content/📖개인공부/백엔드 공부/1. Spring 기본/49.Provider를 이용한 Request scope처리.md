

```java title:"Provider를 이용한 MyLogger 주입 - Controller", hl:16,21
package hello.example.core.web;  
  
import hello.example.core.common.MyLogger;  
import jakarta.servlet.http.HttpServletRequest;  
import lombok.RequiredArgsConstructor;  
import org.springframework.beans.factory.ObjectProvider;  
import org.springframework.stereotype.Controller;  
import org.springframework.web.bind.annotation.RequestMapping;  
import org.springframework.web.bind.annotation.ResponseBody;  
  
@Controller  
@RequiredArgsConstructor  
public class LogDemoController {  
  
    private final LogDemoService logDemoService;  
    private final ObjectProvider<MyLogger> myLoggerProvider;  
  
    @RequestMapping("log-demo")  
    @ResponseBody  
    public String logDemo(HttpServletRequest request){  
        MyLogger myLogger = myLoggerProvider.getObject();  
        String requestURL = request.getRequestURL().toString();  
        myLogger.setRequestURL(requestURL);  
  
        myLogger.log("controller test");  
        logDemoService.logic("testId");  
  
        return "OK";  
    }  
}
```

```java title:"Provider를 이용한 MyLogger 주입 - Service", hl:12
package hello.example.core.web;  
  
import hello.example.core.common.MyLogger;  
import lombok.RequiredArgsConstructor;  
import org.springframework.beans.factory.ObjectProvider;  
import org.springframework.stereotype.Service;  
  
@Service  
@RequiredArgsConstructor  
public class LogDemoService {  
  
    private final ObjectProvider<MyLogger> myLoggerProvider;  
    public void logic(String id) {  
        MyLogger myLogger = myLoggerProvider.getObject();  
        myLogger.log("service id = "+id);  
    }  
}
```

# 자 이제 됐으니 돌려보자

___

http://localhost:8080/log-demo 
![[Pasted image 20241230232511.png]]
![[Pasted image 20241230233333.png]]
- 지금은 로그가 깔끔하게 뜨는데, 많은 요청이 한번에 들어오면 로그의 순서가 좀 꼬이게 된다. -> <font color="#ffff00">그래도 uuid는 유지된다!</font>

___

### 정리

1. 스프링 컨테이너에 올리는 것은 Provider를 통해서 지연시킨다
2. Http 요청이 들어오게 되면, 컨테이너에 들어오기 때문에 정상적으로 호출이 가능

___


# 근데 이제 또 Provider를 쓰는게 귀찮아 진다.. 그냥 처음처럼 선언해서 쓸 수는 없을까??
<font color="#ffff00">-> 프록시!!!</font>
 

