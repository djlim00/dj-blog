
- 앞에서 있었던 상황인 'OrderServiceImple'에서 'DiscountPolicy'인터페이스의 구체인 'FixDiscountPolicy'를 고르는 것은, 
	 <u> 마치 로미오와 줄리엣에서 줄리엣 역할의 사람이 로미오 역할을 고르는 것과 같다.</u>

- 모든 역할은 자신의 역할(관심사)만 해야지, 자신의 책임만을 책임져야함.
- 즉 공연을 구성하고, 담당배우를 섭외하고, 배역을 지정하는 <font color="#d83931">기획자</font>가 필요함

___
## "AppConfig"

- 애플리케이션의 전체 동작 방식을 구성(Config)하기 위해 **"구현객체를 생성"** 하고, **"연결"** 하는 책임을 가니는  <u>별도의 설정 클래스</u>

![[Pasted image 20241122210014.png]]
- 위에서 처럼 구현체를 가져오는 것이 아니라 Repository에 대한 생성자를 만듦.
- 더이상 MemoryMemberRepository라는 구체적인 구현체에 의존하지 않음.

![[Pasted image 20241122210138.png]]
- 그리고 Appconfig에서는 MemoryMemberRepository를 대신 넣어줌.

#### 여기서도 생성자를 만들어주고..
![[Pasted image 20241122210613.png]]
![[Pasted image 20241122210647.png]]
- Appconfig에서 넣어줌


========================================================================
- **AppConfig는 애플리케이션의 실제 동작에 필요한 <span style="background:#d4b106">구현객체를 생성</span>**
- **생성한 객체 인스턴스의 참조(레퍼런스)를 <span style="background:#d4b106">생성자를 통해서 주입(연결)해준다.</span>**
========================================================================


<u></u>## <span style="background:#d4b106">DIP를 지킬 수 있게 된다</span>


___

### 클래스 다이어그램
![[Pasted image 20241122211738.png]]
- 이제는 AppConfig에서 어떤 Imple인지를 정해준다.

### 회원 객체 인스턴스 다이어그램
![[Pasted image 20241122211854.png]]
- memberServiceImpl 입장에서는 의존관계를 마치 외부에서 주입해주는 것 같은 느낌
- 생성자 입장에서는 어떤 구현체가 들어와서 생성될지는 알 수가 없기 때문에
	=> <span style="background:#d4b106">DI (Depency Injection)</span>

___

## MemberApp에서는 어떻게 생성해서 쓸까?
![[Pasted image 20241122212334.png]]
- Appconfig를 생성해서 그것을 통해서 memberService에 들어가는 구현체를 주입받아서 사용한다.

## OrderApp에서는 어떻게 생성해서 쓸까?
![[Pasted image 20241122212748.png]]


이 모든 것은 아래에 전부 써져있다.
## <span style="background:#d4b106">그저 빛 AppConfig</span>
![[Pasted image 20241122212825.png]]

___
## Test Code 수정사항

![[Pasted image 20241122213219.png]]

- **<span style="background:#d4b106">@BeforeEach</span> : 각각의 테스트(@Test)를 수행하기 전에 수행되는 코드를 지정해줌.
- 각각의 코드를 돌리기 전에 Appconfig코드를 만들어줌.
