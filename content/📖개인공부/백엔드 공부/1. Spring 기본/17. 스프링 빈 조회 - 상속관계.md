
- <span style="background:#d4b106">부모타입을 조회하면, 자식 객체들은 전부 다 끌려 나온다.</span>
	- 그래서 모든 자바 클래스의 최상위 클래스인 Object타입으로 조회를 하면 모든 스프링 빈을 전부 조회한다.

#### 상속관계 조회 예시
![[Pasted image 20241123170651.png]]

```java title:"상속관계 빈 조회"
import static org.assertj.core.api.Assertions.*;  
import static org.junit.jupiter.api.Assertions.*;  
  
public class ApplicationContextExtendFindTest {  
  
    AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(TestConfig.class);  
  
  
    @Test  
    @DisplayName("부모 타입으로 조회시, 자식이 둘 이상 있으면, 중복 오류가 발생한다.")  
    void findByParentTypeDuplicate() {  
        // 이렇게 그냥 부모 클래스(DiscountPolicy)로 조회하면 두개의 자식 클래스가 있어서 중복 오류가 발생한다.  
        // DiscountPolicy discountPolicy = ac.getBean(DiscountPolicy.class);        assertThrows(NoUniqueBeanDefinitionException.class, () -> ac.getBean(DiscountPolicy.class));  
    }  
  
    @Test  
    @DisplayName("자식이 둘 이상 있으면, 빈 이름을 지정하면 된다.")  
    void findByParentTypeBeanName() {  
        DiscountPolicy discountPolicy = ac.getBean("rateDiscountPolicy", DiscountPolicy.class);  
        assertThat(discountPolicy).isInstanceOf(RateDiscountPolicy.class);  
    }  
  
    @Test  
    @DisplayName("부모 타입으로 모두 조회하기")  
    void findAllBeanByParent() {  
        Map<String, DiscountPolicy> beansOfType = ac.getBeansOfType(DiscountPolicy.class);  
        assertThat(beansOfType.size()).isEqualTo(2);  
        for (String key : beansOfType.keySet()) {  
            System.out.println("key = " + key + " value = " + beansOfType.get(key));  
        }  
    }  
  
  
    @Test  
    @DisplayName("Object타입으로 모두 조회하기")  
    void findAllBeanByObject() {  
        Map<String, Object> beansOfType = ac.getBeansOfType(Object.class);  
        for (String key : beansOfType.keySet()) {  
            System.out.println("key = " + key + " value = " + beansOfType.get(key));  
        }  
    }  
  
  
    // 구체에 의존하는 코드 - 좋은 방법은 아님  
    @Test  
    @DisplayName("특정 하위 타입으러 조회하기")  
    void findBeanBySubtype() {  
        DiscountPolicy discountPolicy = ac.getBean(RateDiscountPolicy.class);  
        assertThat(discountPolicy).isInstanceOf(RateDiscountPolicy.class);  
    }  
  
  
    // 자식이 두개인 부모 클래스 DiscountPolicy    @Configuration  
    static class TestConfig {  
        @Bean  
        public DiscountPolicy rateDiscountPolicy() {  
            return new RateDiscountPolicy();  
        }  
  
        @Bean  
        public DiscountPolicy fixDiscountPolicy() {  
            return new FixDiscountPolicy();  
        }  
    }  
}
```


- 사실 실제 개발할 때, 빈을 조회할 일은 거의 없다. 그럼 왜?
- <span style="background:#d4b106">기본 기능이기도 하고, 가끔 순수 자바 어플리케이션에서 스프링 컨테이너를 사용해야하는 경우가 있음 -> 그리고 자동 의존관계 주입에서 부모(상속)관계가 어떻게 조회되는지를 알아야하므로</span>