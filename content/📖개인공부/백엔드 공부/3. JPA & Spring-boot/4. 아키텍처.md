![[Pasted image 20250522173122.png]]
- Controller, web : 웹 계층
- Service : 비즈니스 로직, 트랜잭션
- repository : JPA를 직접 사용하는 계층, Entity Manager사용
- domain : 엔티티가 모여있는 계층, 모든 계층에서 사용

___

### 객체생성 방법 create메서드만 가능하게 하기
![[Pasted image 20250523164204.png]]
- 위에 protected 생성자를 하나 만들어버리면 다른 곳에서 new Order()로 새로운 객체 만들 수 없음
- <mark style="background: #FFF3A3A6;">항상 이렇게 제약을 하는 방향으로 만들자(유지보수를 위해서)</mark>

____
### 1. 엔티티에 중요한 비즈니스 로직이 있음 -> "도메인 모델 패턴"
- 서비스에서는 이런 비즈니스 로직의 조합만
### 2. 일반적으로 엔티티에는 로직이 없음 -> "트랜잭션 스크립트 패턴"
- 엔티티에는 Getter, Setter 정도만 있고(update나 create까지)
- 핵심 비즈니스 로직은 Service에 있음

### 심지어 하나의 프로젝트 안에서 "도메인 모델 패턴"과 "트랜잭션 스크립트 패턴"이 동시에 존재할 수도 있음!

___
# JPA에서 동적 쿼리를 어떻게 처리해야하는가?

- 회원명과, 주문 상태를 필터링 해서 가져오는 쿼리를 날려야함..!

```java title:"여기서 필터링을 빼는 동적 쿼리를 만들어야함."
public List<Order> findAll(OrderSearch orderSearch){  
  
    return em.createQuery("select o from Order o join o.member m" +  
                    " where o.status = :status" +  
                    " and m.name like :name", Order.class)  
            .setParameter("status",orderSearch.getOrderStatus())  
            .setParameter("name",orderSearch.getMemberName())  
            .setMaxResults(1000) // 최대 1000건  
            .getResultList();  
}
```
- 지금은 name이랑 status를 모두 조건에 있는 쿼리문인데, 이게 둘 중 하나만 있거나, 없거나, 동적으로 바뀜(사용자가 필터링을 어떻게 하냐에 따라서)

## 1. 하나하나 문자열로 넣기..(condition에 따라서) -> 개에바
- 조건이 있으면 쿼리를 "~" +"~" 막 넣어서 해줘야 함,
- 문자열에서 오타 하나라도 나면 그냥 끝장임.
## 2. JPA Criteria -> 개에바
## 3. Mybatis를 사용하면 동적 쿼리가 편함
## 4. QueryDsl -> 갓갓😎