바로 상세보기로 가면 고객 입장에서는 이제 저장이 제대로 된건지 알기가 쉽지 않다.
-> 사용자 친화적이지 않음!
-> "저장이 되었습니다"라는 문구가 뜨게 하는 방법이 있다.

```java title:"RedirectAttributes를 사용", hl:2,3,4,5,8
@PostMapping("/add")  
public String addItemV6(Item item, RedirectAttributes redirectAttributes){  
    Item savedItem = itemRepository.save(item);  
    redirectAttributes.addAttribute("itemId", savedItem.getId());  
    redirectAttributes.addAttribute("status", true);  
  
    // 남은 status는 쿼리파라미터로 들어가게 된다.  
    return "redirect:/basic/items/{itemId}";  
}
```




![[Pasted image 20250312161927.png]]
- 일단 쿼리파라미터로 넘어오고..

![[Pasted image 20250312162632.png]]
- 타임이프로 대충 수정하면 status가 true면 저장완료가 뜨게 할 수 있다..!

