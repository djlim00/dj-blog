```java title:"미친 @ModelAttribute", hl:23
//@PostMapping("/add")  
public String addItemV1(@RequestParam String itemName,  
                   @RequestParam int price,  
                   @RequestParam int quantity,  
                   Model model){  
    // Set으로 하는게 ModelAttribute의 동작 방식이니까 Set으로 하자  
    //Item item = new Item(itemName,price,quantity);  
    Item item = new Item();  
    item.setItemName(itemName);  
    item.setPrice(price);  
    item.setQuantity(quantity);  
  
    itemRepository.save(item);  
  
    model.addAttribute("item",item);  
  
    return "basic/item";  
}  
  
@PostMapping("/add")  
public String addItemV2(@ModelAttribute("item") Item item){  
    itemRepository.save(item);  
    //model.addAttribute("item",item);  
    return "basic/item";  
}

//@PostMapping("/add")  
public String addItemV3(@ModelAttribute Item item){  
  
    // HelloData --> hellodata  
    // Item --> item    // 이런식으로 이름을 바꿔서 전달함  
    itemRepository.save(item);  
    return "basic/item";  
}  
  
@PostMapping("/add")  
public String addItemV4(Item item){  
  
    // 이런 객체의 경우는 또 @ModelAttribute가 생락된거임!  
    itemRepository.save(item);  
    return "basic/item";  
}

```
- <mark style="background: #FFF3A3A6;">@ModelAttribute를 사용하면 심지어 모델도 필요가 없다!!! 그냥 모델이 넣어줌</mark>

![[Pasted image 20250312104134.png]]
- <span style="background:rgba(3, 135, 102, 0.2)">요청 파라미터의 값도 가져오고,, 모델에 모델 어트리뷰트로 지정한 값을 자동으로 넣어준다!</span>


### 너무 많이 생략하는 것도 마냥 좋은 건 아닌 것 같다..! 😥