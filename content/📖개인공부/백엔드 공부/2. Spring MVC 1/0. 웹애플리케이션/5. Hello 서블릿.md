
스프링 부트 환경에서 서블릿을 등록하고 사용해보자.


```java title:"HelloServlet 예제코드"

package hello.servlet.basic;  
  
import jakarta.servlet.ServletException;  
import jakarta.servlet.annotation.WebServlet;  
import jakarta.servlet.http.HttpServlet;  
import jakarta.servlet.http.HttpServletRequest;  
import jakarta.servlet.http.HttpServletResponse;  
  
import java.io.IOException;  
  
@WebServlet(name="helloServlet",urlPatterns = "/hello")  
public class HelloServlet extends HttpServlet {  
  
  
    @Override  
    protected void service(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {  
        System.out.println("HelloServlet.service");  
        System.out.println("request = " + request);  
        System.out.println("response = " + response);  
  
        String username = request.getParameter("username");  
        System.out.println("username = "+username);  
  
        response.setContentType("text/plain");  
        response.setCharacterEncoding("utf-8");  
        response.getWriter().write("hello "+username);  
    }  
}
```

- Http 요청과 응답을 굉장히 편리하게 받아오고, 편리하게 작성할 수 있다.
- @WebServlet
	- name : 서블릿 이름
	- urlPatterns : url 지정



### 👍 resources/application.properties에 로그 찍히게 하기
![[Pasted image 20250105130749.png]]
추가해주면 
![[Pasted image 20250105130811.png]]
이렇게 요청을 보내면 로그가 터미널에 찍힌다.

❗운영서버에는 부하가 발생할 수 있으니, 개발할 때만 사용하자



### 1. 서블릿의 동작방식
![[Pasted image 20250105130935.png]]
![[Pasted image 20250105130944.png]]

___
# 웰컴 페이지 만들기

- main/webapp 디렉토리를 만들고
	- index.html을 만든다. -> 웰컴 페이지
	- 거기에 링크를 만들어서 basic.html로 리다이렉트 되게 해놓았다.

___

