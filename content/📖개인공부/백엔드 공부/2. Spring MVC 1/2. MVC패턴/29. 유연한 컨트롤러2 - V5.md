# 이제 V4도 구현해보자

![[Pasted image 20250222183946.png]]
- V4핸들러 어댑터가 생겼다 -> 이건 마치 V4의 프론트컨트롤러와 같은 역할

```java title:"FrontControllerV5 - V4 추가"
private void initHandlerAdapters() {  
    handlerAdapters.add(new ControllerV3HandlerAdapter());  
    handlerAdapters.add(new ControllerV4HandlerAdapter());  
  
}  
  
private void initHandlerMappingMap() {  
    // 우선 V3를 지원부터 만듬  
    handlerMappingMap.put("/front-controller/v5/v3/members/new-form",new MemberFormControllerV3());  
    handlerMappingMap.put("/front-controller/v5/v3/members/save",new MemberSaveControllerV3());  
    handlerMappingMap.put("/front-controller/v5/v3/members",new MemberListControllerV3());  
    // V4 추가  
    handlerMappingMap.put("/front-controller/v5/v4/members/new-form",new MemberFormControllerV4());  
    handlerMappingMap.put("/front-controller/v5/v4/members/save",new MemberSaveControllerV4());  
    handlerMappingMap.put("/front-controller/v5/v4/members",new MemberListControllerV4());  
}
```
- V4를 위한 핸들러 어댑터를 추가해주고
- V4의 핸들러 매핑을 위한 url을 추가해준다.

```java title:"ContrllerV4HandlerAdapter"
public class ControllerV4HandlerAdapter implements MyHandlerAdapter {  
    @Override  
    public boolean supports(Object handler) {  
        return (handler instanceof ControllerV4);  
    }  
  
    @Override  
    public ModelView handle(HttpServletRequest request, HttpServletResponse response, Object handler) throws ServletException, IOException {  
        // 이미 supports 에서 한번 ControllerV3인지 거르기 때문에 캐스팅 해도 안전하다  
        ControllerV4 controller = (ControllerV4) handler;  
  
        Map<String, String> paramMap = createParamMap(request);  
        HashMap<String, Object> model = new HashMap<>();  
  
        String viewName = controller.process(paramMap, model);  
  
        ModelView mv = new ModelView(viewName);  
        mv.setModel(model);  
  
        return mv;  
    }  
  
    private static Map<String, String> createParamMap(HttpServletRequest request) {  
        Map<String, String> paramMap = new HashMap<>();  
        request.getParameterNames().asIterator()  
                .forEachRemaining(paramName -> paramMap.put(paramName, request.getParameter(paramName)));  
        return paramMap;  
    }  
  
  
}
```
- 기존의 V4의 FrontContoller와 거의 같은 모습
- <span style="background:rgba(205, 244, 105, 0.55)">그렇지만 기존에는 URL값만을 return했다면</span>
	- v5의 핸들러 어댑터는 모델뷰를 반환해야 하기 때문에
	- 핸들러(Controller)를 자여와서 -> paramMap을 createParamMap()으로 만들고
	- model에 들어갈 정보를 담기 위해서 modelHashMap을 만든다.
	- process()를 통해서 model에 값이 들어가게 되고,
	- 모델 뷰를 핸들러 어댑터에서 만들어져서
	- 반환된다.




- 우리는 이번에 V4를 추가하면서 기존의 코드를 바꾸는 부분이 핸들러 목록을 넣어주는 부분 밖에 없다 -> 이것도 심지어 밖에서 주입하게 되면 OCP를 완전히 지킬 수 있다!!
- 우리는 V4를 추가하고 싶으면 그냥 V4를 위한 코드를 추가한 것 말고는 달라진게 없음
- <font color="#d83931">어댑터 패턴을 추가하면 이렇게 유연해 질 수 있다~</font>

### 역할과 구현의 구분 🫡


