
- 지금 요청 파라미터 정보만 필요하지 req, res가 필요한게 아니다.
- <font color="#ffff00">서블릿 기술이 없어도 동작할 수 있음.</font>
	- 지금은 사용하지도 않는 HttpServlerRequest req과 같은 코드가 필요가 없다.

1. 뷰 이름의 경로가 거의 동일한데, 중복이 일어나고 있음 "/WEB-INF....."
2. <font color="#ffff00">물리적인 경로를 넣는 것이 아니라, members, save-result, new-from만 써도 되도록 함</font>
	- 나중에 뷰의 폴더 위치가 통채로 바뀌어도, 프론트 컨트롤러에서 경로만 바꿔주면 된다.
3. 핵심적인 비즈니스 로직만 남기고 싶음..

### V3의 구조
![[Pasted image 20250131150801.png]]
- Controller에서 모델+뷰인 ModelView를 반환한다.
- viewResolver에서 물리적인 이름으로 변환을 해서 MyView객체를 반환해서 돌려줌
- 이번 버전에는 HttpServletRequest를 사용할 수가 없음 -> Model이 별도로 필요함!

```java title:"뷰모델"
package hello.servlet.web.frontcontroller;  
  
import lombok.Getter;  
import lombok.Setter;  
  
import java.util.HashMap;  
import java.util.Map;  
  
@Getter @Setter  
public class ModelView {  
    private String viewname;  
    private Map<String, Object> model = new HashMap<>();  
  
    public ModelView(String viewname) {  
        this.viewname = viewname;  
    }  
}
```


