
# 메시지

- 만약에 프로젝트에 "상품명"이라는 단어를 모두 "상품이름"으로 바꿔야 된다고 해보자.
	- 하드코딩된 모든 "상품명"을 수십개의 파일을 고쳐야 한다.

<span style="background:#b1ffff">이러한 문제를 해결하기 위해서 다양한 메세지를 한 곳에서 관리하도록 하는 기능을 메시지 기능이라고 한다.</span>

![[Pasted image 20250527164501.png]]
![[Pasted image 20250527164513.png]]
- 작은 프로젝트에서는 중요하지 않지만, 프로젝트 크기가 커지면 중요함!

___
# 국제화

- <mark style="background: #FFF3A3A6;">위에서 설정한 message.properties를 각 나라별로 별도로 관리하면 서비스를 국제화 할 수 있음.</mark>

```c title:"message_en.properties"
item=Item
item.id=Item ID
item.itemName=Item Name
item.price=price
item.quantity=quantity
```

```c title:"message_ko.properties"
item=상품
item.id=상품 ID
item.itemName=상품명
item.price=가격
item.quantity=수량
```

한국에서 접근한 것인지 영어에서 접근한 것인지는 인식하는 방법은 HTTP `accept-language` 해더 값을 사용하거나 
사용자가 직접 언어를 선택하도록 하고, 쿠키 등을 사용해서 처리하면 된다.

- 스프링은 기본적으로 메시지와 국제화를 통합해서 제공한다! 😃

___
# Spring이 제공하는 메세지-국제화 기능

![[Pasted image 20250528081243.png]]
- 기본이 Spring.messages.basename=messages 가 되어 있는데, 추가적인 설정을 좀 더 해준거임
##### 기본 설정 관련 Spring 공식 문서
https://docs.spring.io/spring-boot/appendix/application-properties/index.html


![[Pasted image 20250528143320.png]]
- 위와 같이 `th:text="#{...}"` 으로 넣어주면 된다.



![[Pasted image 20250528143252.png]]
- 기본적으로는 브라우저의 언어 설정에 따라서 하지만
	- 그와 별개의 설정을 통해서 언어 설정에 대한 정보를 쿠키나, 세션에 넣어서 확인 하는 방법도 있음.

![[Pasted image 20250528143454.png]]
- LocaleResolver의 구현체를 @Override해서 나만의 방법으로 언어 설정을 바꿀 수 있다.
	- 예를 들어서 팝업창을 띄우고 선택에 따라 세션이나 쿠키에 저장을 하면 되니까.