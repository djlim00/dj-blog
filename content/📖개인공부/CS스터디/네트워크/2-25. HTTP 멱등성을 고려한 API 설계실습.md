
### <span style="background:#fff88f">POST 요청의 경우는 idempotency Key(멱등성 키)를 보내서 동일한 요청인지를 식별!</span>
- 동일한 작업의 재시도를 인식하고 한 번만 처리되도록 함
- 중복 트랜잭션을 피해야하는 결제 처리 API에서 자주 사용됨

![[Pasted image 20250224230106.png]]



![[Pasted image 20250224230309.png]]
- idempotency-key를 확인을 해야함
	- 없다면 에러
	- 이미 있다면, 동일한 요청임을 출력
![[Pasted image 20250224231242.png]]
-> curl 요청을 통해서 HEAD요청의 결과를 보니! Idempotency-Key가 잘 나가고 있음!

## 멱등성이 중요한 이유
1. <span style="background:#fff88f">일관성</span> : 보통 반복되는 요청이 단일 요청과 동일한 효과를 갖도록 보장하는 것은 시스템 전반에 걸쳐 일관된 상태를 유지하는데 도움이 됩니다.
2. <span style="background:#fff88f">유지보수성</span> : 멱등성을 가진 HTTP 메서드로 API로 통신을 하는데 있어서 재시도를 처리할 때의 사이드이펙트를 고려한 복잡한 로직을 구현할 필요가 없기 때문에 이 부분을 잘 고려해서 설계한다면 유지보수성이 증가