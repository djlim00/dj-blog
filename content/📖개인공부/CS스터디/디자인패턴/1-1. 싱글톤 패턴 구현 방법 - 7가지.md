싱글톤 패턴은 하나의 클래스는 하나의 인스턴스만 만든다. 
하나의 인스턴스로 여러개의 모듈에 공유한다.

# 1. 단순한 getInstance() 호출 - <font color="#ff0000">원자성 결여</font>
___
![[Pasted image 20250206133339.png|400]]
### 문제점 - 원자성 결여

- 자바는 멀티 쓰레드 언어이다 -> 여러개의 쓰레드가 사용될 때를 고려해야한다.
	- Th1과 Th2가 둘다 getInstance()를 호출하면
	- 무조건 쓰레드가 순차적으로 가는게 아니다 -> <span style="background:#fff88f">쓰레드는 경쟁상태</span>라서 
	- 그렇게 되면 Th1이 if(instance== null까지한 상태로 Th2로 가게 되면 
	- 둘다 Singleton객체를 생성할 수도 있다
=> <font color="#ff0000">싱글톤 패턴이 깨짐</font>



# 2. 단순한 getInstance()+synchronized키워드 - <font color="#ff0000">성능저하</font>
___
![[Pasted image 20250206134518.png|500]]
- synchronized키워드는 lock을 거는 키워드
- 최초로 접근한 스레드가 해당 메서드 호출시에 lock을 획득함
- <font color="#ff0000">싱글톤 패턴 구현이 가능하다 But!!! lock이 걸려있어도, getInstance()메서드를 호출하는 것은 가능하기 때문에 성능이 저하된다.</font>
	- 여러개의 모듈들이 계속 getInstance()를 호출할건데, 계속 lock을 잡는건 성능이 크게 저하됨.




# 3-4. 정적 멤버 & 정적 블럭(static) - <font color="#ff0000">자원낭비</font>
___
#### 3. 정적 멤버
![[Pasted image 20250206140717.png|500]]

#### 4. 정적 블럭
![[Pasted image 20250206140926.png|500]]

static 멤버나 블록은 런타임이 아니라, 최초에 JVM이 클래스 로딩 때 모든 클래스들을 로드할 떄 미리 인스턴스를 생성함
클래스 로딩과 동시에 싱글톤 인스턴스를 만든다
-> 모듈들이 싱글톤 인스턴스가 필요하면 getInstance()에서 new Singleton()으로 생성하는게 아니라 그냥 만들어져서 정적공간에 있는 인스턴스를 반환만 하면 됨.
#### 문제점
: <font color="#ff0000">무조건 클래스가 로드될 때 모든 인스턴스가 생성되어서 자원을 먹고 있음</font>



# 5. 정적 멤버와 Lazy Holder(중첩 클래스) - <font color="#00b050">자원낭비 방지</font>
___
![[Pasted image 20250206141046.png|500]]

- 기존에는 JVM이 클래스를 로드하면서 static 인스턴스가 생성되었는데,
- static 인스턴스를 나중에 모듈이 호출하면 할당하는 방식을 사용하여서 자원 낭비를 줄인다.



# 6. 이중 확인 잠금(DCL, Double Checked Locking)
____
- 인스턴스 생성 여부를 
- 싱글톤 패턴 잠금 전에 한번
- 객체를 생성하기 전에 한번
- 총 2번 체크하면
- 인스턴스가 존재하지 않을 때만 잠금을 걸 수 이기 때문에 앞서 생겼던 문제를 해결!
![[Pasted image 20250206141513.png|500]]
1. "instance== null"로 인스턴스가 있는지 없는지 확인
2. synchrinize 키워드로 한번 더 잠금

### ⚠ volatlile 키워드

우선 메모리 구조는 다음과 같다.
1. 메인 메모리 위에, CPU 캐시메모리라고 불리는 L3, L2, L1 캐시가 있음(가끔 L4)
![[Pasted image 20250206141833.png]]

- 자바는 스레드가 여러개가 열리면, RAM(메인메모리)에서 메모리를 가져오는 것이 아니라.
- 캐시 메모리에서 각각의 캐시메모리를 기반으로 가져옴!
![[Pasted image 20250206163028.png|500]]
- <span style="background:#fff88f">각각의 캐시메모리에서 가져오기 때문에, Thread간 변수가 공유가 안되버림..!</span>

# 예제 코드
![[Pasted image 20250206164032.png]]
- 이 코드를 보면 분명히 같은 변수인 flag를 사용하지만, 첫번째 Thread에서의 while문이 끝나지 않는다. -> 각각의 캐시 메모리를 기준으로 돌고 있기 때문에

![[Pasted image 20250206164125.png]]
- 이렇게 volatile 키워드를 이용하면 flag변수가 RAM에 올라가서 공유가 되기 때문에 while문이 sleep(100)이후 종료된다.


### <font color="#00b050">volatile 키워드를 사용하면, 메인### 메모리(RAM)에 변수를 올림! => 공유 가능</font>



# 7. enum
___

- enum의 인스턴스는 기본적으로 Thread safe함이 보장되기 때문에 이를 통해 인스턴스를 생성가능.
 
![[Pasted image 20250206171925.png]]





# 좋은 방법 - 5. static+lazyHolder / 7. enum
___

- 5번과 7번을 추천드립니다. 5번은 가장 많이 쓰인다고 알려져있고 7번은 이펙티브 자바를 쓴
조슈아 블로크가 추천한 방법입니다.
"A single-element enum type is the best way to implement a singleton
Joshua Bloch, Effective Java 2nd Edition p.18"

