총 20문제
db - 8문제(10)
db심화 - 8문제(10)
프론트엔드의 이해 - 4문제(5)

# 1. 프론트엔드의 이해
____
1. **다음 중 HTML의 semantic태크가 아닌 것은 무엇인가?**
	- <head/> -> <header/><span style="background:rgba(3, 135, 102, 0.2)">가 semantic태크고 이건 아님</span>
	- <footer/>
	- <nav/>
	- <aside/>

2. **다음 중 HTML의 <input/> 태그의 type이 아닌 것은 무엇인가**
	- password
	- button
	- radio
	- <span style="background:rgba(3, 135, 102, 0.2)">select -> 이건 독립된 얘임 input에 포함되지 않음</span>

3. **다음 중 HTML의 테이블 태그가 아닌 것은 무엇인가?**
	- <thead/>
	- <tbody/>
	- <tsection/> - <span style="background:rgba(3, 135, 102, 0.2)">이런거 없음!</span>
	- <tfoot/>

4. **다음 <form/>  태그의 속성 중에서, form submit을 할 때 어디로 form-data를 보낼 것인지 명시하는 것은 무엇인가?**
	- id
	- name - 서버에서 참조하는 이 element의 이름
	- <span style="background:rgba(3, 135, 102, 0.2)">action</span> - 데이터를 보내는 곳
	- method

5. **다음 중 웹 문서를 사용자가 알아보기 쉽게 꾸미거나, 사용하기 편리하도록 배치할 떄 사용하는 기술은?**
	- HTML
	- <span style="background:rgba(3, 135, 102, 0.2)">Cascading Style Sheets</span> - CSS
	- Javascript
	- Spring framework


# 2. 데이터 베이스
____

1. **다음 중 데이터베이스의 특징으로 올바르지 않은 것은?**
	- <span style="background:rgba(3, 135, 102, 0.2)">데이터베이스는 실시간으로 서비스되어서는 안된다.</span>
	- 데이터 값은 다양한 사용자에 의해 바뀔 수 있다
	- 데이터베이스는 서로 다른 업무 또는 여러 사용자에게 동시에 공유된다
	- 데이터베이스에 저장된 데이터는 데이터의 물리적인 위치가 아니라 데이터 값에 따라 참조한다.

2. **다음 중 DBMS에 저장된 테이블 구조를 정의하는 데 사용하는 SQL의 종류는 무엇인가?**
	- DCL : 유저 만들고, grant, revoke
	- <span style="background:rgba(3, 135, 102, 0.2)">DDL : CREATE, ALTER</span>
	- DML : INSERT, DELETE...
	- DQL : Data Query Language

3. **다음 중 기본키로 선정되지 않은 후보키를 무엇이라고 하는가?**
	- 슈퍼키(Super Key)
	- 기본키(PK)
	- <span style="background:rgba(3, 135, 102, 0.2)">대체키(alternate key)</span> - Super key에서 PK를 제외한 것
	- 외래키(FK)

4. **SELECT 문의 문장 프레임워크 중에서, 질의의 어느 테이블이 사용되는지 열거하는 곳은 다음 중 어디인가.**
	- SELECT
	- <span style="background:rgba(3, 135, 102, 0.2)">FROM</span>
	- WHERE
	- ORDER BY

5. **SELECT 절에서 사용하는 중복제거 키워드는 무엇인가?**
	- ALL
	- <span style="background:rgba(3, 135, 102, 0.2)">DISTINCT</span>
	- SUM
	- AVG

6. **다음의 WHERE 절 중에서 price 속성이 NULL인 행을 찾는 표현으로 올바른 것은?**
	- where price = NULL;
	- where price int (NULL);
	- where price not in (NULL);
	- <span style="background:rgba(3, 135, 102, 0.2)">where price is NULL;</span> - NULL은 연산 자체가 안됨

7. **다음 중에서 price 속성의 총 합계를 구할 때 사용할 수 있는 그룹 함수는 무엇인가?**
	- <span style="background:rgba(3, 135, 102, 0.2)">sum(price)</span>
	- max(price)
	- avg(price)
	- min(price)

8. **MySQL에서 사용하는 다음의 데이터 타입 중에서, 고정 길이 문자형으로, 문자를 저장하고 남은 공간은 공백으로 채우는 특성을 가진 데이터타입은 무엇인가?**
	- int
	- <span style="background:rgba(3, 135, 102, 0.2)">char</span>
	- numeric
	- decimal

9. **Book 테이블에 isbn 속성을 추가하는 다음의 DDL 문장 중에서 올바른 것은 무엇인가?**
	- <span style="background:rgba(3, 135, 102, 0.2)">alter table bood add isbn varchar(13);</span>
	- table altered book add isbn varchar(13);
	- book table altered add isbn varchar(13);
	- table book add isbn varchar(13) altered;

10. **다음 중 NoSQL DBMS는?**
	- Oracle
	- MySQL
	- PosgreSQL
	- <span style="background:rgba(3, 135, 102, 0.2)">MongoDB</span>


# 데이터베이스 심화
___
1. **개념, 논리, 물리 모델링** + 장문의 보기 -> <span style="background:rgba(3, 135, 102, 0.2)">명확하게 틀린 답이 하나가 있음</span>

2. **정규화 문제** -> 종속성, 2/3 정규화

3. **트랜잭션 ACID** -> 고립성, 원자성 개념

4. **dirty read, non-repeatable read, phantom read**

5. **다음은 INDEX에 대한 설명이다. 올바른 것은?(MySQL 기준)**
	- INDEX는 PK칼럼에 대해서 자동으로 만들어 지지만, FK 컬럼에 대해서는 자동으로 만들어지지 않는다. (X)
	- INDEX는 해당 컬럼을 가진 테이블의 다른 컬럼에서 만들어진다. (X, 다른 컬럼이 아님)
	- <span style="background:rgba(3, 135, 102, 0.2)">SELECT에 대한 이득이 생기지만, INSERT, UPDATE, DELETE에 대한 손해가 생긴다. (O, 가급적 업데이트가 없는 테이블에 인덱스를 잡아야함)</span>
	- 다양한 값을 가지는 컬럼보다 '남', '여' 두 값만 가지는 성별에 위한 컬럼 등에 INDEX를 추가한다. (X)

6. **JOIN - 명확한 틀린 보기를 찾아라.**

7. **다음 SUBQUERY에 관련한 설명 중 틀린 것은?**
	- <span style="background:rgba(3, 135, 102, 0.2)">ORDER BY 절에 사용할 수 없다. -> 할 수 있음!!</span>
	- SELECT 절에 사용할 수 있다.
	- WHERE 절에 사용할 수 있다.
	- SUBQUERY 내에서 사용할 수 있다.

8. **FK - 점선/실선, 강한/약한** -> <span style="background:rgba(3, 135, 102, 0.2)">명확한 틀린 보기를 찾아라.</span>

9. **다음 LIKE 검색에 대한 설명이 올바르지 않은 것은?**
	- 시작하는 단어가 일치하는 검색은 LIKE+[검색어] + % 로 검색한다.
	- 단어를 초함하는 검색은 LIKE + % + [검색어] + % 로 검색한다.
	- LIKE가 사용되는 컬럼이 INDEX가 있다면 항상 그 INDEX를 이용한 검색이 실행된다.
	- LIKE 검색을 할 때 특정 자리수까지 조건을 넣으려면 '_'을 이용한다.

10. **EXIST 문제**