- Compose와 swarm은 둘다 Dockerfile로 한다.

- 워크 플로우 : 이미지 빌드/풀, 환경변수, 스케일링
- 사용성 : 개발/테스트에서 빠른 정의-실행

swarm이 더 느림 -> 모든 서비스를 레지스트리에 올려서 해야해서.
compose -> local에서 사용하는 것이 목적이지만 swarm 처럼 여러 노드에서 사용가능함

Compose
1. 단일 서버 개발/테스트, 소규모 단일 호스트 운영
2. 로컬 빌드/디버깅 빠른 반복

Swarm
- 여러 노드에 복제/분산, 내장 LB/fhffld 업데이트/배치