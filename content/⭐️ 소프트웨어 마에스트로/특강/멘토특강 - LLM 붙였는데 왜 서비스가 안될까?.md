- 멀티모달
- LLM을 의존하지 않고, 내가 원하는 방식으로 변형할 수 있을까?
	- RAG
	- Fine Tuning
- 사용자에게 다가가는 방법으로는 agent 방식으로 바뀌게 되었다.

- 개발속도가 굉장히 단축됐다 -> Language 쪽도 구현이 굉장히 빨라진다 -> <font color="#ffc000">즉 "방향성"이 중요해졌다.</font>
1. **대규모 모델들(gpt, gemini)들의 파라미터가 이제는 1조개가 넘어간다.**
	- Fine tuning이 너무 과해진다
	- 맞춤형으로 만들기가 너무 힘들어진다.
	- 기본적인 POC단계에서는 좋지만 -> 실질적인 서비스를 만드는 것에 있어서 제한이 된다.
		- 상용화가 어려워진다.
2. **아무생각없이 개발 시작**

## 실패 사례
### 1. 리걸(legal) 테크 사례
- 법+행정
- 판례 검색 + 판례 이해 + 판례 요약 (정말 수십만건이 있다.)
- 전처리가 필요함
	- 사실 판례를 검색하고 -> 판례를 <font color="#ffc000">이해</font>하고 -> 판례를 요약해야함 / 그래야지 매번 모든 DB를 볼 필요가 없어짐.
- 데이터를 인코딩하고 나서(인코딩, <font color="#ffc000">앞선 예시의 "판례 이해"의 측면 ex.BERT</font>) -> 생성을 해야함(디코더 ex.GPT)
1. 도메인 특화 언어 문제(도메인 Fine Tuning 필요 -> 데이터 필요)
2. 최신성/할루시네이션 문제(RAG)
3.  요약방식에 따른 모델 선택
	- 추출 요약 : BERT계열 -> 인코딩
	- 추상 요약 : GPT계열
	- 유사 판례 검색 -> 유사도 분류 BERT

##### AI의 학습(training)
- 이게 이제 pre-training / fine tuning(adaptation) 으로 나누어진다.
- <u>레이블링된 고가 데이터가 없기 때문에 나누어진 것이다.</u> (정답이 없기 때문에)
##### AI의 추론모델(Inference, Reasoning) : 

##### LLM서비스의 주의점 3H
1. **Honest**(할루시네이션이 있으면 안됨) -> RAG(설계를 잘 해야함) 
2. **Harmless**(유해해서는 안됨) -> 프롬프트 <u>가드레일</u> 
3. **Helpful**(유용성이 있어야함) -> 제대로 동작이 잘 되야함.

### 2. 소라 서비스 중단
- 이미지는 비용이 너무 큼,,
- 수익성이 되나?
- 그렇다면 Vision을 포기했을 때 -> 솔직히 딱히 피해가 없을 수도 있다.
	- Vision 데이터는 이미지, 영상 -> <font color="#ffc000">이게 결국 언어로 바뀐다.</font>
- 실질적인 서비스는 언어적인 측면이 좋고, 마케팅 측면은 비전쪽도 좋다.



#### 대안1. RAG
- 이것도 최신의 데이터가 굉장히 필요함 -> 파이프라인에서 피딩을 해주는,,
#### 대안2. Fine Tuning
- 데이터가 없음 -> 프로젝트가 난관이 부딪힌다 -> 걍 시작이 안됨ㅋㅋ

## 소마 팀이 쓸 수 있는 Fine-tuning
- Full FT는 너무 힘듦
### LoRA / QLoRA
![[Pasted image 20260412151818.png|300]]
- 만개 혹은 천개의 파라미터만 나의 데이터로 학습시킨다
- 소마 5개월에 현실적인 선택 LoRA or RAG

# 교훈 및 판단 기준 비교 요약

### 1. 구현 범위를 줄이고, 핵심 하나를 제대로 만들어라
### 2. 라벨이 없으면 만들거나, 없이도 되는 방법을 찾아라.
### 3. 사용자 강화학습 RLHF -> DPO
- 차별화가 될 수 있는 방식
- 휴면 레이블, 휴먼 어노테이터, 휴먼 레이블

___

![[Pasted image 20260412151648.png]]
![[Pasted image 20260412152036.png]]


## 결국 데이터가 굉장히 중요함..

## 더 일어나서는 안되는 오류에 대해서 생각해야함
![[Pasted image 20260412153602.png]]
- <mark class="hltr-yellow">암환자인데 암환자가 아니라고 하는 오류는 무조건 막아야함.🔥🔥🔥🔥🔥</mark>
	- 그 다음으로는 암환자가 아닌데 암환자라고 하는 것을 막아야함.
- Threshold




